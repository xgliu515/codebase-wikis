# Trace 导览总览：一条消息的完整旅程

参考手册的 15 章把 Hermes Agent 拆成一个个子系统讲清楚。但子系统之间是怎么咬合的、一条用户消息从键盘到屏幕到底流过哪些代码——靠翻参考手册是拼不出来的。这份 trace 导览就是为此存在：它选定**一个最小但真实的请求**，沿着调用栈一步步走完整条链路，每一步都回链到参考手册对应的小节。

读完参考手册你知道每个零件长什么样；走完这趟导览你知道这台机器是怎么转起来的。

---

## 选定的 trace 目标

我们追踪这样一次交互——它足够小（只触发一次工具调用），又足够真实（端到端真的能跑通），而且**穿过了全部四层架构**：

```text
$ hermes
（进入交互式 CLI，光标停在输入框）

你 ▸ 读取 README.md 并告诉我它的第一行是什么

（agent 调用 read_file 工具读文件，再组织一句回答）

Hermes ▸ README.md 的第一行是一个居中的 <p> 标签，里面是项目 banner 图片。
```

为什么选它：

- **最小复杂度**：只用到一个工具 `read_file`，不涉及子 agent、MCP、压缩、多平台等高级特性——这些留给参考手册。
- **真实**：这是任何用户装好 Hermes 后第一分钟就会做的事，不是为讲解硬造的场景。
- **穿透四层**：入口层（`hermes` 脚本 → CLI）、Agent 核心（对话循环）、模型与上下文（Provider 调用 + 系统提示）、工具与扩展（工具注册 + 执行）——一层都不缺。如果选的目标让某一层"空转"，那一层的 trace 步骤就会写不出东西。

整条链路会触发**两次** LLM API 调用：第一次模型决定"我需要读文件"并返回一个 `tool_call`；工具执行后把结果回灌，第二次模型才生成给用户看的自然语言回答。这个"想 → 调工具 → 再想"的循环正是 agent 的本质，导览的第 10–16 步会把它拆到最细。

---

## 8 段式模板

从第 1 步起，每一步都用固定的 8 段结构写。这个结构的目的，是让每一个设计决策都显得**像是某个真实问题的逻辑结果**，而不是一句空降的结论。

| # | 段落 | 它负责什么 |
|---|------|-----------|
| 1 | 当前情境 | 锚定你在系统里的位置：上一步结束时什么数据结构成型了 |
| 2 | 问题 | 这一步必须解决的具体需求，带上利害关系 |
| 3 | 朴素思路 | 凭直觉你会怎么做——要让读者觉得"换我也这么写" |
| 4 | 为什么朴素思路会崩 | 具体的失败模式，不是"性能差"，而是"差在哪、错在哪" |
| 5 | Hermes 的做法 | 此时真实设计读起来就像水到渠成的答案 |
| 6 | 代码位置 | 按阅读顺序列出的 `file:line` 引用 |
| 7 | 分支与延伸 | 链接回参考手册——把线性 trace 接进知识网络 |
| 8 | 走完这一步你脑子里应该多了什么 | 3–5 条以"新知识"措辞的收获 |

代码版本锁定在 `NousResearch/hermes-agent@f36c89cd5`（2026-05-17）。所有 `file:line` 都基于这个 commit。

---

## 20 步速览

| 步 | 标题 | 一句话 | 主要落在 |
|----|------|--------|---------|
| 01 | 敲下 hermes 之后 | shell 脚本如何把控制权交给 Python | 入口层 |
| 02 | 参数解析与子命令分发 | `main()` 怎样路由到 `cmd_chat` | 入口层 |
| 03 | 构造 HermesCLI | `cli.py::main` 与三路配置加载 | 入口层 |
| 04 | 创建 AIAgent | `_init_agent` 把 60 个参数交给 `init_agent` | Agent 核心 |
| 05 | 工具自动发现 | AST 扫描 `tools/*.py` 填满注册表 | 工具与扩展 |
| 06 | 拼装系统提示 | SOUL.md、工具定义、记忆拼成一段稳定文本 | Agent 核心 |
| 07 | REPL 读取用户输入 | `run()` 主循环拿到那行字 | 入口层 |
| 08 | 进入 run_conversation | `chat()` 启动同步对话循环 | Agent 核心 |
| 09 | 记忆 prefetch 与消息构造 | 转折前回忆 + 组装 `api_messages` | Agent 核心 |
| 10 | 第一次 LLM API 调用 | provider adapter + 凭证池发出请求 | 模型与上下文 |
| 11 | 解析 tool_call | 模型回了一个 `read_file` 调用 | Agent 核心 |
| 12 | 工具调度 | `tool_executor` → `handle_function_call` | 工具与扩展 |
| 13 | read_file 实际执行 | `file_tools` 读文件、审批检查 | 工具与扩展 |
| 14 | 工具结果回灌 | tool 消息追加，循环回绕 | Agent 核心 |
| 15 | 第二次 LLM API 调用 | 带工具结果再问一次模型 | 模型与上下文 |
| 16 | 纯文本响应与循环退出 | 没有 tool_call，循环结束 | Agent 核心 |
| 17 | 流式渲染输出 | spinner 收起，response box 打印 | 入口层 |
| 18 | 会话持久化 | `SessionDB` 把消息写进 SQLite | 模型与上下文 |
| 19 | 收尾：标题生成与记忆 sync | 后台辅助 LLM 任务 | 模型与上下文 |
| 20 | 回到 REPL | 一轮完成，等待下一条输入 | 入口层 |

---

## 状态演化表

trace 的精髓是看**状态如何随每一步累积**。下面这张表记录关键变量在每一步结束时的样子——相邻步骤的 agent 用它对齐，不会互相矛盾或重复背景。

| 步 | 进程/对象状态 | `messages` 列表 | 关键新增变量 |
|----|--------------|----------------|-------------|
| 01 | Python 解释器启动，`hermes_cli.main` 已导入 | — | — |
| 02 | `args` 解析完毕，`args.func = cmd_chat` | — | `args` |
| 03 | `HermesCLI` 实例化，配置字典就位 | — | `cli.config` |
| 04 | `AIAgent` 实例化，SessionDB 连接建立 | — | `cli.agent` |
| 05 | `ToolRegistry` 装满 ~79 个工具条目 | — | `registry._tools` |
| 06 | 系统提示字符串成型并缓存 | `[system]` | `system_prompt` |
| 07 | REPL 收到用户那行文本 | `[system]` | `user_input` |
| 08 | 进入 `run_conversation`，循环计数器归零 | `[system, user]` | `api_call_count=0` |
| 09 | 记忆块拼好，`api_messages` 组装完成 | `[system, user]` | `api_messages` |
| 10 | 第一次 API 返回，含一个 `tool_call` | `[system, user, assistant(tool_call)]` | `response` |
| 11 | `tool_call` 解析出 `read_file` + 参数 | 同上 | `tool_calls[]` |
| 12 | 调度器选定 handler，准备执行 | 同上 | `task_id` |
| 13 | `read_file` 返回 JSON 字符串结果 | 同上 | `tool_result` |
| 14 | tool 消息追加，`api_call_count=1` | `[..., tool]` | — |
| 15 | 第二次 API 返回，纯文本 | `[..., tool, assistant(text)]` | `response2` |
| 16 | 无 tool_call，循环 break | 同上 | `final_response` |
| 17 | 终端打印出 response box | 同上 | — |
| 18 | 会话与消息写入 `sessions`/`messages` 表 | 同上（已落盘） | `session_id` |
| 19 | 标题生成、记忆 sync 在后台线程跑 | 同上 | — |
| 20 | 回到 `run()` 循环顶，等待输入 | 同上（保留在内存） | — |

---

## 怎么用这份导览

- **顺着读**：从第 1 步走到第 20 步，你会建立起对整条链路的肌肉记忆。
- **跳着读**：每一步的第 6 段给了精确 `file:line`，第 7 段链回参考手册——遇到想深挖的子系统，顺着链接跳进对应章节。
- **对照源码读**：导览锁定了 commit，所有行号可在 GitHub 上一键跳转验证。

下一步：[Trace 步骤 01 —— 敲下 hermes 之后，谁在跑？](tour-01-shell-entry.md)
